# 강의 개념 ↔ 코드 구현 매핑

> 모바일프로그래밍 강의에서 배운 Dart/Flutter 개념이 Albi 코드 어디에 나타나는지 정리.
> file:line 은 작성 시점 기준 — 변경 시 grep 으로 재확인.

## 1. Dart 언어 기초

| 강의 개념 | 코드 증거 |
|-----------|-----------|
| 변수/타입 (final/const, 제네릭 명시) | 전반 — `ref<T>()` 명시, `final` 필드 |
| Null safety (`?` `??` `!`) | `parse_result.dart`, `draft_review_viewmodel.dart` nullable 필드 |
| sealed class + exhaustive switch | `lib/core/exceptions.dart:5` `sealed class AppError` |
| enum | `lib/viewmodels/stats_viewmodel.dart:6` `enum StatsPeriod` |
| 컬렉션 if / spread | 위젯 트리 조건부 children |

## 2. 함수형 프로그래밍

| 강의 개념 | 코드 증거 |
|-----------|-----------|
| `map` | `lib/data/drink_log_repository.dart:75,267,293` |
| `where` / `toSet` / `toList` | `drink_log_repository.dart`, `lib/core/providers.dart` |
| `fold` (집계) | `lib/integrations/drift_demo/condition_database.dart` 평균 계산 / 통계 reduce |
| arrow function | 전반 (`=>` 단문) |

## 3. OOP

| 강의 개념 | 코드 증거 |
|-----------|-----------|
| 상속 (extends) | `lib/viewmodels/*.dart` `extends AsyncNotifier` / `StateNotifier` |
| 인터페이스 (implements) | `exceptions.dart:5` `implements Exception` |
| named constructor / factory | `parse_result.dart:59` `factory ParseResult.empty` |
| 불변 + copyWith | `parse_result.dart:37,100` `copyWith`, `draft_review_viewmodel.dart` |
| 캡슐화 (`_` private) | 전반 (`_db`, `_save` 등) |

## 4. Widget & Lifecycle

| 강의 개념 | 코드 증거 |
|-----------|-----------|
| StatelessWidget / StatefulWidget / ConsumerWidget | 전 화면 |
| `initState` | `entry_card_widget.dart:41`, `input_section_widget.dart:40`, `condition_log_screen.dart` |
| `didUpdateWidget` | `entry_card_widget.dart:57`, `input_section_widget.dart:47` |
| `dispose` (리소스 정리) | `entry_card_widget.dart:97`, `input_section_widget.dart:55` |
| `setState` | `condition_log_screen.dart` (FutureBuilder 재실행), 각 StatefulWidget |
| 레이아웃 (Row/Column/Expanded/Padding/Container) | 전 화면 위젯 트리 |
| ListView.builder | `log_list_screen.dart`, `condition_log_screen.dart` |
| Scaffold/AppBar/FAB | 전 화면 |

## 5. 비동기 프로그래밍

| 강의 개념 | 코드 증거 |
|-----------|-----------|
| `Future` / `async` / `await` | 전 Repository/ViewModel |
| `Stream` | Drift `watch()` — `condition_database.dart:watchAll` |
| **FutureBuilder** | `condition_log_screen.dart` 평균 숙취도 카드 |
| **StreamBuilder** | `condition_log_screen.dart` 목록, `account_screen.dart` sync 상태 |
| Riverpod AsyncValue.when | `stats_screen.dart`, `log_list_screen.dart` 등 |

## 6. Navigation (EffectiveNavigator)

| 강의 개념 | 코드 증거 |
|-----------|-----------|
| Named Routes 상수 | `lib/core/app_routes.dart:22` `class Routes` |
| `onGenerateRoute` 중앙 라우팅 | `lib/app.dart:35` + `app_routes.dart:44` |
| 인자 타입검증 + 에러 fallback | `app_routes.dart` `args is int` + `RouteErrorScreen` |
| pop(result) / push | 보조 화면 진입 |

## 7. 데이터 저장

| 강의 개념 | 코드 증거 |
|-----------|-----------|
| SharedPreferences | `app_settings_viewmodel.dart`, `theme_mode_viewmodel.dart`, `notification_settings_viewmodel.dart` |
| sqflite (메인 DB) | `lib/core/database/database_helper.dart` 8테이블 |
| **Drift (반응형 ORM)** | `lib/integrations/drift_demo/condition_database.dart` (격리 데모) |
| secure storage | AI Key — `ai_settings_viewmodel.dart` |

## 8. 개발 방법론 (AI 시대 / SDD)

| 강의 개념 | 산출물 |
|-----------|--------|
| SDD (Spec-Driven Development) | `specs/albi-mvp/{spec,plan,tasks,test}.md` |
| AI 디자인 워크플로우 | 디자인 토큰/아이콘/리디자인 (WS-E) + `docs/reviews/` |
| AI 페어(Claude/Codex) | 전 워크스트림 Codex 페어 리뷰 |
