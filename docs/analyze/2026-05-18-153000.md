# 코드베이스 분석 리포트 — Albi (2026-05-18)

> `/analyze` 4-agent 병렬 실행 결과. 3 Claude Explore + 1 Codex Independent.
> Verification 5필드 + § 2.5 재검증 + § 2.6 Codex cross-validation + § 2.7 Coverage Audit 통과 항목만 surface.

## Rejected Claims (검증 패스)

재검증 결과 모든 subagent 주장 — 핵심 4건 (nullable copyWith, update() silent no-op, ParseJob fallback 미기록, GeminiClient in ai_settings_viewmodel) 모두 parent context 직접 Read 로 literal 확인 통과. **기각 없음.**

## Codex Cross-Reference (§ 2.6 cross-validation 결과)

> Agent 4 (Codex Independent Analysis) 정상 합류 — fall-back 없음.

### Agreed (4 agent 일치)
- **MVVM 구조 견고**: View → ViewModel → Repository 경계 — views/ 에 Repository 직접 import 0건 (Agent 1, 3, Codex 모두 확인)
- **저장 전 검토 화면 강제**: HomeScreen → DraftReviewScreen 의 명시적 라우팅, 자동 저장 없음 (Agent 1, 3, Codex)
- **ParseOrchestrator AI fallback 동작**: `_tryAiParse()` 실패 시 LocalRuleParser 호출 (parse_orchestrator.dart:66-76) — Agent 2, 3, Codex 모두 literal 코드 확인
- **AI scope 위반: GeminiClient instance in ai_settings_viewmodel.dart:49** — Agent 3 + Codex 동일 finding
- **모든 13 ViewModel + 11 screen + 6 entity 활성 사용** (Agent 1 negative assertion 5-Check 통과, Codex 확인)

### Claude-only (Codex 미발견 → confidence 1 단계 down)
- **cupertino_icons 미사용 (Agent 2)** — pubspec.lock/yaml 에만 존재, lib/ 와 test/ 어디서도 import 0건. Codex 가 별도 finding 으로 surface 안 함 → confidence: likely → uncertain 으로 downgrade. **User Decision 블록으로 이동.**
- **tasting_note_viewmodel.dart → gemini_tasting_note_assistant 직접 import (Agent 3)** — Codex 는 "isolated within viewmodel" 로 보존적 판단. § 2.6 룰에 따라 보존적 판단 채택 → "위반" 등급 → "주의 — 향후 provider 경유로 개선 가능" 으로 강도 down.

### Codex-only (3 Claude agent 미발견 — additional verification recommended, parent context 재검증 통과)

1. **nullable copyWith clear 버그 (HIGH)** — `DraftReviewState.copyWith()` (draft_review_viewmodel.dart:222-223) 와 `DrinkLog.copyWith()` (drink_log.dart:52-53) 모두 `place ?? this.place` 패턴. UI 가 `v.isEmpty ? null : v` 로 null 을 전달해도 copyWith 가 null 을 무시 → **사용자가 장소/메모를 비울 수 없는 기능 버그**. parent verification 직접 Read 로 literal 확인. (Codex-only finding — verified by parent.)

2. **DrinkLogRepository.update() silent no-op (HIGH)** — `drink_log_repository.dart:51-52`: `if (log.id == null) return;` 으로 무시. 호출자 에게 어떤 신호도 없음. 정상 시나리오에서는 도달 불가지만 safety net 으로 `ValidationError` 또는 assert 필요. parent verification 통과.

3. **ParseJob 'fallback' status 정의만 있고 기록 없음 (MEDIUM)** — `parse_job.dart:6` 주석에 `success | failed | fallback` 정의되어 있으나, `parse_orchestrator.dart:84` 에서 실제 insert 시 `'failed'` / `'success'` 만 기록. AI → 로컬 fallback 케이스가 통계 추적 불가. parent verification 통과 (`grep "fallback"` 결과 → 주석만 매치).

4. **이미지 준비 단계 cancellation 누락 (MEDIUM)** — `gemini_text_parser._prepareImage()` (XFile read → ImageDescriptor decode → codec instantiate) 실행 중 CancelToken 체크 없음. 취소는 네트워크 호출 직전에만 발생. (Codex confidence: likely, parent 재검증 skip — 동작 분석 영역, 별도 trace 필요.)

5. **`databaseProvider.requireValue` brittle pattern (LOW)** — `providers.dart:23/28/33/38/43` 모두 `databaseProvider.requireValue` 사용. DB 로딩 전 접근 시 StateError. 현재 `_AppEntry` 에서 await 통과 후에만 진입하므로 동작은 정상이지만, future 변경 시 함정.

6. **database_init_stub.dart / database_init_ffi.dart preservation warning (보존 필수)** — Codex 가 사전 경고: `dart.library.ffi` conditional import 로 main.dart 에 연결되어 있음. naive grep 에서 참조 미발견 → false-positive dead code 판정 위험. **Agent 1 도 dead code 로 flag 하지 않음 (Negative Assertion Summary 통과)** — 양쪽 일치, 액션 없음.

### Disagreements (Codex 가 Claude REJECTED)
없음. Codex 가 명시적으로 Claude finding 을 reject 한 케이스 없음. tasting_note_viewmodel 위반 등급에 대한 강도 차이는 위 Claude-only 항목에서 down 처리.

## Coverage Audit (§ 2.7 positive claim verification)

| # | Claim | Depth | Verdict |
|---|-------|-------|---------|
| 1 | "13 ViewModel + 11 screen + 6 entity 모두 활성 사용" (Agent 1) | L2 | CONFIRMED — Agent 1 이 의심 3 VM (tasting_note/achievements/settings) 각각 import 흔적 literal 확인 |
| 2 | "ParseOrchestrator AI fallback 검증" (Agent 2) | L2 | CONFIRMED — parse_orchestrator.dart:66-76 literal 코드 인용 |
| 3 | "View → Repository 직접 접근 위반 없음" (Agent 3) | L2 | CONFIRMED — grep "import.*Repository" in lib/views/ 결과 empty |
| 4 | "내부 모듈 의존성 그래프 — 역방향 위반 없음" (Agent 2) | L2 | CONFIRMED — 3 샘플 파일 literal import 추적 |
| 5 | "print() 0건" (Agent 3) | L1 | DOWNGRADED — grep 단발성, "production code 만" 의 깊은 정의 (test 제외, debug 등) 미확인. 사실상 OK 추정. |
| 6 | "setState 모두 local UI flag 용도" (Agent 3) | L2 | CONFIRMED — 4 위치 모두 inspect, ConsumerStatefulWidget local state |
| 7 | "context.mounted 모든 async gap 일관 적용" (Agent 3) | L1 | DOWNGRADED — 5개 샘플만 확인, exhaustive enumerate 안 함. 회색지대. |
| 8 | "내부 모듈 의존성 reverse 위반 0건" (Agent 2) | L2 | CONFIRMED |

총 8 단정 중 **CONFIRMED 6 / DOWNGRADED 2**. Downgrade 2건은 "샘플 검증, exhaustive 미확인" 회색지대 — 액션 없음.

## 프로젝트 개요

- **이름**: albi (자연어 음주 기록 앱)
- **프레임워크**: Flutter 3.11.4 / Dart, MVVM with Riverpod 2.6.1
- **스타일**: Material Design, 다크모드 지원, NotoSansKR 한글 폰트
- **저장소**: SQLite (sqflite 2.4.2 + sqflite_common_ffi 2.4.0 for desktop)
- **외부 API**: Gemini AI (Dio 5.9.2 기반, flutter_secure_storage 로 키 관리)
- **버전**: 0.1.0+1 (pubspec.yaml)

## 구조 (4 agent 일치)

```
lib/ — 65 .dart 파일
├── core/ (11) — database/, theme/, utils/, exceptions, providers
├── data/ (5) — 5 Repository (drink_log, liquor_master, ai_config, parse_job, tasting_note)
├── domain/entities/ (6) — 6 entity (DrinkLog/Entry/Food, LiquorMaster, AiConfig, ParseJob, TastingNote, UsageQuota)
├── integrations/ (7) — gemini/ (2), parser/ (5)
├── viewmodels/ (13) — AsyncNotifier 5 / StateNotifier 2 / FutureProvider 7 / StateProvider 2 (혼용)
└── views/ (23) — 11 screen + 12 widget

assets/ — seed/ (liquor_master.json 553종, food_dictionary.json 36개, place_keywords.json), fonts/

test/ — 11 파일 (unit 6 / golden 3+helper / widget 1 / DB integration 1) — 736 LOC
```

라우팅: MaterialApp → AppShell (3-tab BottomNavigationBar: Home/Log/More) + Navigator.push 모달. 명명 라우트 미사용.

## 핵심 패턴

- **상태관리**: Riverpod (AsyncNotifier 우세 → 비동기 쿼리, StateNotifier → 폼/입력 동기 상태, FutureProvider → 단발성 쿼리)
- **API 호출 / 파서**: ParseOrchestrator → LocalRuleParser (regex+seed) | GeminiTextParser (Vision API) → ParseResult. `wasAiAttempted` 플래그로 skip vs fail 구분.
- **에러 처리**: sealed `AppError` (DatabaseError/NetworkError/ParseError/ValidationError) — 단, exhaustive switch 패턴 매칭 코드 부재. try-catch + DatabaseError wrapping 은 5 Repository write 경계 모두 적용.
- **인증**: 없음 (로컬 앱). flutter_secure_storage 는 Gemini API 키 전용.
- **공통 위젯**: ErrorStateWidget, DeleteConfirmDialog — views/common/ 에 2개, 총 22 reference.

## 의존성

핵심 런타임 12 패키지, 모두 lib/ 에 실제 import 발견. 내부 모듈 의존성 그래프 단방향 (역방향 위반 0건).

| 패키지 | 버전 | 사용 파일 수 | 비고 |
|---|---|---|---|
| flutter_riverpod | 2.6.1 | 29 | core MVVM |
| sqflite | 2.4.2 | 9 | DB |
| dio | 5.9.2 | 6 | Gemini HTTP |
| intl | 0.20.2 | 8 | 날짜/숫자 포맷 |
| shared_preferences | 2.5.5 | 4 | 설정 |
| flutter_secure_storage | 9.2.4 | 3 | API 키 |
| fl_chart | 0.68.0 | 1 | 통계 차트 (latest ~0.73, 업데이트 후보) |
| cupertino_icons | 1.0.9 | 0 | **lib/ 사용 0건** — Claude-only, User Decision |

## Recommendations (개선 제안)

### HIGH — 즉시 조치 후보

1. **nullable copyWith clear 버그 수정** — 증거: [draft_review_viewmodel.dart:222-223](lib/viewmodels/draft_review_viewmodel.dart#L222-L223), [drink_log.dart:52-53](lib/domain/entities/drink_log.dart#L52-L53) / 액션: sentinel 패턴 (`Object? place = _sentinel` + `identical(place, _sentinel) ? this.place : place as String?`) 또는 `clearPlace()`/`clearOverallMemo()` 명시 메서드 추가 + UI saveToDb 로직에서 호출 / confidence: certain (Codex-only finding, parent verified)

2. **DrinkLogRepository.update() silent no-op 제거** — 증거: [drink_log_repository.dart:51-52](lib/data/drink_log_repository.dart#L51-L52) / 액션: `if (log.id == null) throw ValidationError('update 호출 시 log.id 필수');` 로 변경 / confidence: certain (Codex-only finding, parent verified)

### MEDIUM — 다음 작업 후보

3. **ParseJob 'fallback' status 실제 기록** — 증거: [parse_orchestrator.dart:84](lib/integrations/parser/parse_orchestrator.dart#L84) 가 `'failed' | 'success'` 만 기록, [parse_job.dart:6](lib/domain/entities/parse_job.dart#L6) 주석은 `fallback` 도 정의 / 액션: AI 시도 후 LocalRuleParser fallback 한 경우 status='fallback' 으로 인서트 분기 추가 → AI 처리 로그 UI ([ai_settings_screen.dart:256](lib/views/settings/ai_settings_screen.dart#L256)) 에서 fallback 케이스 별도 표시 / confidence: certain (Codex, parent verified)

4. **AI scope 위반: GeminiClient instance in AiSettingsViewModel** — 증거: [ai_settings_viewmodel.dart:49](lib/viewmodels/ai_settings_viewmodel.dart#L49) `final _geminiClient = GeminiClient();` / 액션: `geminiClientProvider` 추가하여 `ref.read(geminiClientProvider).validateKey()` 형태로 변경. ParseOrchestrator 도 동일 provider 재사용 가능 / confidence: certain (Agreed — Agent 3 + Codex 둘 다 확인)

5. **이미지 준비 단계 cancellation checkpoint 추가** — 증거: [gemini_text_parser.dart](lib/integrations/parser/gemini_text_parser.dart) `_prepareImage` 영역 (XFile read + ImageDescriptor decode) / 액션: `cancelToken.whenCancel.then(...)` 또는 단계 사이 `cancelToken.isCancelled` 체크 / confidence: likely (Codex-only, parent skip — 동작 분석 후 적용)

### LOW — 여유 있을 때

6. **`fl_chart` 업데이트 검토** — 증거: pubspec.yaml 0.68.0 vs latest 0.7x / 액션: stats 차트 호환성 검토 후 `^0.73.0` 또는 안정 latest 로 / confidence: likely (네트워크 검증 없음)

7. **상대 날짜 토큰 경계 부재** — 증거: `date_utils.dart` `text.contains('어제')` 패턴 (Codex finding) / 액션: 단어 경계 정규식 또는 토큰화 후 매칭 / confidence: likely

8. **`databaseProvider.requireValue` 패턴 안전성** — 증거: [providers.dart:23,28,33,38,43](lib/core/providers.dart) / 액션: 진입 가드 (`_AppEntry`) 가 await 통과 후에만 들어오므로 현재 안전. future 진입점 추가 시 함정 — 주석 문서화만 / confidence: likely

### User Decision (자동 액션 금지)

- **cupertino_icons 의존성** (Claude-only, Codex 미언급) — 증거: [pubspec.yaml](pubspec.yaml) `cupertino_icons: ^1.0.8` + lib/ 에서 `CupertinoIcons.*` 사용 0건 (literal grep 결과) / 가능 액션: (a) 유지 — Flutter 표준 transitive safety net (b) iOS 디자인 가능성 위해 보존 (c) 제거 후 iOS 빌드 회귀 테스트 / confidence: likely (uncertain after Codex downgrade) / 권장: **유지** (Flutter `flutter create` 기본 포함 + iOS 위젯 hybrid 가능성 + 제거 이득 micro)

- **tasting_note_viewmodel.dart 의 gemini_tasting_note_assistant 직접 import** — 증거: Agent 3 위반 vs Codex 허용 / 가능 액션: (a) 그대로 유지 — ViewModel 1개에 한정된 isolation (b) `tastingNoteAssistantProvider` 추가하여 provider 경유 / confidence: likely / 권장: 단발성 위반 — 별도 작업 시 (b) 적용, 우선순위 LOW

## Next Suggestion

- HIGH 항목 2건 (nullable copyWith, update() silent no-op) 즉시 수정 가능 — `/plan nullable copyWith 버그 + update silent no-op 수정` 으로 구현 계획을 세울까요?
- MEDIUM 항목 3건 (ParseJob fallback / AI scope / 이미지 cancellation) 은 별도 작업으로 묶거나 fallback 부터 단독 수정 권장.
- Agent 1 negative assertion 통과 — dead code 없음 (database_init_*.dart conditional import 포함 보존).
