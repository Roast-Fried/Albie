# UI 검토 리포트 — 캡처 화면 (Claude 시각 + Codex 코드 페어)

> 2026-06-02 / 방법: Claude 가 캡처 PNG 17+ 화면(light+dark) 시각 검토 + Codex 가
> 화면 .dart 코드 레벨(a11y/레이아웃/카피/상태/M3) 검토 → 교차 검증.
> 대상: full_light/dark 01-11, ca_calendar/account/notification, archive_03_detail,
> seeded_04(log_detail)/06-07(tasting)/12-13(dialog), onboarding 01-03.

## 총평
앱 UI 는 **전반적으로 완성도 높음** — 일관된 M3 cream/amber 테마, 마스터매칭 자동완성
(✓+영문명+자동 도수), 차트/테이스팅노트/다이얼로그/온보딩/다크테마 모두 양호.
주요 개선 여지는 **접근성(a11y)** 과 **에러 상태 재시도 동선**, **개발자용 카피**.

집계: HIGH 4 · MEDIUM 18 · LOW 11 (Codex) + 캡처/시각 finding 5 (Claude).

## 교차 검증 (Claude 시각 ↔ Codex 코드)
| 항목 | Claude(시각) | Codex(코드) | 판정 |
|------|-------------|-------------|------|
| account dormant 카피 | ".env/다시 빌드/docs..." 사용자 화면 노출 | account_screen.dart:305 개발자용 문구 | **Agreed** |
| 파괴적 액션 색 | 초기화 "삭제" amber | archive 즐겨찾기 `Colors.red` 하드코딩 + error 토큰 권장 | **Agreed(테마)** |
| 영어 혼용 카피 | "재방문 reminder" | settings:57 / notification:54 reminder·follow-up | **Agreed** |
| 캘린더 마커 | 작은 색 점만 | calendar_screen:85 마커 Semantics 부재 | **Agreed** |

## HIGH (4, 전부 a11y)
1. **테이스팅 노트 편집 터치타겟** — `tasting_note_section.dart:71` InkWell+Padding(4)+Icon(16) ≈ 24dp → 48dp IconButton + tooltip/Semantics.
2. **통계 차트 Semantics** — `stats_screen.dart:237` PieChart/LineChart 시각만 → `Semantics(label:'주종별 분포: 기타 67%...')` + 텍스트 요약.
3. **AI 처리 로그 상태** — `ai_settings_screen.dart:264` 성공/실패 아이콘 색만 → "성공/실패" 텍스트 또는 Semantics.
4. **캘린더 마커 a11y** — `calendar_screen.dart:85` 색 점만 → `calendarBuilders` 로 날짜 셀 `Semantics(label:'6월 2일, 기록 2건')`.

## MEDIUM (선별)
- **레이아웃 overflow 위험**: `log_list_screen.dart:225` 음식+장소 Row, `archive_detail_screen.dart:256` 수량+장소 Row — `Flexible`+ellipsis 필요(360dp 긴 텍스트).
- **터치타겟 고정 높이**: 홈 칩 `SizedBox(36)`(home:112), 아카이브 FilterChip `SizedBox(40)`(archive:39) → 48dp.
- **에러 상태 재시도 부재**: settings:30, ai_settings:32(원문 예외 노출), notification:18 → `ErrorStateWidget(onRetry)`.
- **날짜 변경 행 a11y**: `draft_review_screen.dart:311` InkWell <48dp + Semantics 부재.
- **저신뢰 entry 표시**: `entry_card_widget.dart:153` border 색만 → "확인 필요" 텍스트/아이콘 병행.
- **account 진행중 Semantics**: account_screen.dart:95 spinner 만 → "로그인 처리 중" 라벨.
- **온보딩 overflow/인디케이터**: onboarding:31 내부 스크롤 부재, :42 점 인디케이터 Semantics 부재.

## LOW (선별)
- **영어 혼용 카피**: "재방문 reminder"/"follow-up" → 한국어 통일 (settings:57, notification:54).
- **개발자용 카피**: `account_screen.dart:305` ".env/다시 빌드/docs/..." → 일반 사용자 문구로(개발 안내는 debug variant).
- **하드코딩 색**: archive `Colors.red`(:207), ai_settings 빈 로그 `Colors.grey`(:237) → `colorScheme.error/onSurfaceVariant`.
- **IconButton tooltip 전반 누락**: 홈 알림(tooltip 'AI 처리 로그'인데 AI설정 이동 — 불일치), log 검색/상세 수정·삭제, ai_settings 키 보기/저장.
- **에러 live region**: `error_state_widget.dart:11` `Semantics(liveRegion:true)` 권장.

## Claude 시각 전용 finding (캡처/UX)
| # | 유형 | 내용 |
|---|------|------|
| C1 | 캡처 | seeded_04_log_detail 에 AppBar "기록 상세" 누락(body-only) — 앱 정상, 캡처 한계 |
| C2 | 캡처 | archive_03_detail 이 **상세 아닌 목록(다크)** 캡처 → archive_detail 시각검토 불가(재캡처 권장) |
| C3 | UX | 데이터 초기화 "삭제" 버튼 amber → error/red(파괴·복구불가 강조) |
| C4 | UX | 홈 AI off 시 "AI로 생성" 비활성(grey) — 합리적이나 숨김/툴팁이 더 명확 |

## 잘 된 점
- home/calendar/account: `AsyncValue.when` 로딩/에러/데이터 분기 + ErrorStateWidget 재시도.
- log_list: RefreshIndicator + 빈 상태 CTA + reduce-motion AnimatedSwitcher.
- stats: 빈 상태 카피 + 차트 색 ColorScheme 파생.
- 마스터매칭 UX(✓+영문명+자동 도수), 온보딩 가치제안+예시, 다크 테마 패리티.
- AppearAnimation `disableAnimations` 존중.

## 최우선 TOP 6 (수정 권장 순)
1. tasting_note 편집 버튼 48dp IconButton (HIGH/a11y)
2. 통계 차트 Semantics 요약 (HIGH/a11y)
3. AI 처리 로그 성공/실패 텍스트 (HIGH/a11y)
4. 캘린더 마커 셀 Semantics (HIGH/a11y)
5. log_list/archive_detail Row Flexible+ellipsis (MEDIUM/overflow)
6. account dormant 개발자 카피 → 사용자 문구 + 초기화 "삭제" error 색 (LOW/UX, 사용자 체감)
