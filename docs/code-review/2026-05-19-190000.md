# Code Review Report — Push 된 4 commit (a103f6c..e5786aa)

> 실행 시각: 2026-05-19 19:00
> Scope: 본 세션 push (58 파일, +3855 -1295)
> commit: chore(.gitignore) + feat(Phase 2.5 closure) + test(회귀+golden) + docs(sync)

## Summary

| 카테고리 | Critical | High | Medium | Low | 합계 |
|---|---|---|---|---|---|
| Lint | 0 | 0 | 0 | 0 | **0** |
| Type | 0 | 0 | 0 | 0 | **0** (flutter analyze pass) |
| Security | 0 | 2 | 2 | 2 | **6** |
| Codex | — | — | — | — | background 대기 |
| Architecture | 0 | 1 | 2 | 4 | **7** |
| **합계** | **0** | **3** | **4** | **6** | **13건 (Codex 미포함)** |

회귀 통과: analyze 0 issues + unit 83 tests + golden 7 + device E2E. **Critical 0건 — push 차단 아님**.

## High (3건) — 즉시 fix 권장

### SEC-001 — DioException.message PII leak (likely)
- **severity**: high
- **file**: [lib/integrations/parser/parse_orchestrator.dart:195](lib/integrations/parser/parse_orchestrator.dart#L195)
- **description**: `'AI 분석 실패: ${e.message}'` 가 `lastErrorMessage` 로 DB 저장 → 설정 화면 평문 노출. Dio 5.x 의 일부 에러 경로에서 `e.message` 가 URL (key 포함) 또는 응답 body 포함 가능.
- **fix**: `e.message` interpolation 제거 + status code 별 정적 한국어 메시지로 통일.
- **autoFixable**: yes

### SEC-002 — home_viewmodel e.toString() UI leak (uncertain edge)
- **severity**: high
- **file**: [lib/viewmodels/home_viewmodel.dart:105](lib/viewmodels/home_viewmodel.dart#L105)
- **description**: `state = state.copyWith(error: e.toString())` — 비-AppError DioException rethrow 시 URL (key 포함) UI 노출 가능. 현 코드 흐름은 unreachable 이나 defense-in-depth.
- **fix**: `e is AppError ? e.userMessage : '입력을 처리하지 못했습니다'` 분기.
- **autoFixable**: yes

### ARCH-001 — View → integrations 직접 의존 (layering 위반)
- **severity**: high
- **file**: [lib/views/log/widgets/tasting_note_edit_sheet.dart:4,248-272](lib/views/log/widgets/tasting_note_edit_sheet.dart#L4)
- **description**: `import 'integrations/parser/gemini_tasting_note_assistant.dart'` 로 `TastingNoteEnhanceReason` enum 직접 사용. View 는 viewmodel 만 알아야 함.
- **fix**: enum 을 `viewmodels/tasting_note_viewmodel.dart` 로 이전 + integrations 는 string 또는 별도 enum 사용 → viewmodel mapping.
- **autoFixable**: 부분 (enum 이전 + Outcome 재구성)
- **위반 룰**: 의존성 방향

## Medium (4건)

### SEC-003 — image picker error raw interpolation
- **file**: [lib/viewmodels/home_viewmodel.dart:65](lib/viewmodels/home_viewmodel.dart#L65)
- `error: '사진을 불러오지 못했습니다: $e'` → 정적 메시지로 통일. PlatformException 은 일반적으로 PII 없으나 일관성.

### SEC-004 — place_keywords regex 미 escape
- **file**: [lib/integrations/parser/local_rule_parser.dart:457](lib/integrations/parser/local_rule_parser.dart#L457)
- `RegExp('(\\S+$suf)')` — `suf` 가 현재 dev-controlled JSON 라 안전. 향후 사용자 입력 source 추가 시 ReDoS / regex injection 표면. `_escapeRegExp(suf)` 적용.

### ARCH-002 — Rule 8 cleanup 로직 중복
- **file**: drink_log_repository.delete 트랜잭션 vs parse_job_repository.clearRawByLogId
- delete 트랜잭션 안에 raw cleanup 인라인 vs 외부 함수 — 정책 drift 위험.
- **fix**: txn 받는 helper 추출 또는 동일 SQL 상수화.

### ARCH-003 — integrations layer 의 raw Exception 9개
- **file**: gemini_text_parser.dart (52,55,62,67,75,85,143) + gemini_tasting_note_assistant.dart (67,76)
- `throw Exception('...')` raw 사용. catch 가 ParseError/NetworkError 로 wrap 하지만 source 일관성 부족.
- **fix**: `throw ParseError(...)` 또는 `throw NetworkError(...)` 직접 사용.

## Low (6건)

| ID | severity | file | 내용 |
|---|---|---|---|
| SEC-005 | low | [lib/main.dart:44,67-103](lib/main.dart#L44) | `_CrashFallback(message:)` 전달 받지만 미사용 — 필드 제거 또는 debug-only |
| SEC-006 | low | [lib/core/database/sqflite_row_ext.dart:6](lib/core/database/sqflite_row_ext.dart#L6) | `StateError` 메시지에 `$v` (DB row value) 포함 — schema 위반 시만 도달, low |
| ARCH-004 | low | [lib/viewmodels/tasting_note_viewmodel.dart:70-85](lib/viewmodels/tasting_note_viewmodel.dart#L70) | Outcome.reason 이 integrations enum 그대로 노출 (ARCH-001 와 묶음) |
| ARCH-005 | low | [lib/viewmodels/app_settings_viewmodel.dart:35,43,53](lib/viewmodels/app_settings_viewmodel.dart#L35) | SharedPreferences setString/setBool try-catch 없음 (Rule 6 정신 확장) |
| ARCH-006 | low | [lib/integrations/parser/parse_orchestrator.dart:85](lib/integrations/parser/parse_orchestrator.dart#L85) | parseJob.rawRequest 가 사용자 확정 전 저장 — 검토 취소 시 stale 잔류 (전체 삭제 시만 정리) |
| ARCH-007 | low | [lib/integrations/parser/gemini_text_parser.dart:169](lib/integrations/parser/gemini_text_parser.dart#L169) | `warningsForMissingLiquorNames` `@visibleForTesting` 누락 (이전 세션 결정 — dartdoc 유지) |

## Per-Rule Verdict (CLAUDE.md 8 rule)

| 룰 | 상태 |
|---|---|
| 1. 저장 전 검토 화면 | PASS |
| 2. AI 로직 integrations/parser/ 안 | PASS |
| 3. AI 없어도 동작 (LocalRuleParser fallback) | PASS |
| 4. failure path first | PASS (tasting_note + home + delete 모두 finally/cleanup) |
| 5. View → Repository 직접 X | PASS (grep 0 hits) |
| 6. Repository 쓰기 try-catch + DatabaseError | PASS (신규 3 메서드 모두) |
| 7. autoDispose 미사용 default | PASS (grep 0 hits) |
| 8. 개인정보 cleanup | PASS w/ ARCH-002 (중복 로직) |

## Verified Strengths (재확인)

- PII cleanup atomic (drink_log delete 트랜잭션 + parseJob.clearRawByLogId)
- Release-mode debugPrint 완전 차단 (kDebugMode guard 4건)
- SQL 100% parameterized (`?` placeholder + whereArgs, 인터폴레이션 0)
- Regex `_escapeRegExp` helper 정의 + alias 단글자 적용 (suf 만 누락 SEC-004)
- API key flutter_secure_storage (EncryptedSharedPreferences / Keychain default)
- EXIF auto strip (PNG 재인코딩)
- Retry 401/429 제외 (rate-limit amplification 차단)
- CancelToken rethrow 정확
- pubspec 신규 의존성 (image_picker 1.2.2 / cross_file 0.3.4+2) known CVE 0
- 신규 viewmodel cross-import acyclic DAG (sinks: achievements, stats, log_list)
- kAlbiAmber seed + golden 회귀 통과
- 83 unit tests 신규 18+ 추가 (LocalRuleParser bug A/B/C/D + retry + standard drink + SqfliteRow + privacy)

## Action Items

- [ ] **HIGH (3건)** — SEC-001 / SEC-002 / ARCH-001 즉시 fix 권장 (다음 commit)
- [ ] **MEDIUM (4건)** — SEC-003 / SEC-004 / ARCH-002 / ARCH-003 다음 sprint
- [ ] **LOW (6건)** — 여유 있을 때

## Codex review

> Background 진행 중. 도착 시 본 문서에 합치.
